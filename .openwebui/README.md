The content of the pipe file needs to be copy&pasted into a custom function.

The `agents` folder contains preconfigured agents (with custom system prompts etc.) and can be imported into Open Web UI.
